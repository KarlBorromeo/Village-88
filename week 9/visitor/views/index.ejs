<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
        <style>
            div{
                position: absolute;
                top: 50%;
                left: 50%;
                transform: translate(-50%,-50%);
                text-align: center;
            }
            a{
                text-decoration: none;
                color: white;
                background-color: blue;
                padding: .2rem .4rem;
                border-radius: 3px;
            }
        </style>
    </head>
    <body>
        <div>
            You are visitor #<%= counter %>
            <% if(counter%2 === 0){%>
                <p>Even Flowers need rain</p>
            <% }else{ %>
                <p>Beat the odds</p>
            <% } %>
            <a href="/repeat">Repeat</a>
            <a href="/reset">Reset</a>
        </div>
        
    </body>
</html>